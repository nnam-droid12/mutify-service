spring:
  datasource:
    username: ${SPRING_DATASOURCE_USERNAME}
    password: ${SPRING_DATASOURCE_PASSWORD}
    url: ${SPRING_DATASOURCE_URL}
    driver-class-name: org.postgresql.Driver


  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
      file-size-threshold: 2KB
      enabled: true




  mail:
    host: smtp.gmail.com
    port: 587
    username: techandrewwilliam@gmail.com
    password: kxrd hizk yfry meiy
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true




azure:
  speech:
    key: ${AZURE_SPEECH_KEY}
    region: ${AZURE_SPEECH_REGION}

  storage:
    connection-string: ${AZURE_STORAGE_CONNECTION-STRING}
    container-name: ${AZURE_STORAGE_CONTAINER-NAME}

jwt:
  key: ${JWT_KEY}


server:
  port: 5050






